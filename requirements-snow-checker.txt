# Snow Checker Lambda 专属依赖（最小集，勿用完整 requirements.txt）
# cryptography 必须钉在 38.0.4：更新版本改用 Rust 构建，需要 GLIBC 2.28，
# 而 Lambda python3.10 运行时（Amazon Linux 2）只有 2.26——装不上会导致
# pip 静默失败、打出空包（2026-07 踩过的坑）
cryptography==38.0.4
firebase-admin==6.2.0
psycopg2-binary==2.9.9
requests==2.31.0
